en:
  dry_validation:
    errors:
      rules:
        username:
          exists?: Already exists
        email:
          exists?: Already exists
        password_confirmation:
          eql?: Password confirmation must match password
        deadline:
          future?: must be in the future
        image:
          supported_format?: Unsupported attachment format
