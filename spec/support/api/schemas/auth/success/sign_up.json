{
  "id": "file:/success/sign_up.json",
  "type": "object",
  "required": ["data"],
  "properties": {
    "data": {
      "type": "object",
      "required": ["signUp"],
      "properties": {
        "signUp": {
          "type": "object",
          "required": ["csrf", "access", "refresh"],
          "properties": {
            "csrf": {"type": "string"},
            "access": {"type": "string"},
            "refresh": {"type": "string"}
          }
        }
      }
    }
  }
}
